# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=custify2025

# JWT Secret (should be a strong random string in production)
JWT_SECRET=custify-admin-secret-key-2025

# Vercel KV (Redis) - for storing demo submissions
# Get these from Vercel dashboard > Storage > KV
KV_URL=your-kv-url
KV_REST_API_URL=your-kv-rest-api-url
KV_REST_API_TOKEN=your-kv-rest-api-token
KV_REST_API_READ_ONLY_TOKEN=your-kv-read-only-token

# Database (optional - for future use)
# DATABASE_URL=postgresql://username:password@localhost:5432/custify

# Email (optional - for future notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
